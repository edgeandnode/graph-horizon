// Default config
export { config } from "./arbitrum-one"

// All configs (for testing or multi-network)
export { config as arbitrumOneConfig } from "./arbitrum-one"
export { config as testConfig } from "./test"

// Types
export { NetworkConfig } from "./types"
