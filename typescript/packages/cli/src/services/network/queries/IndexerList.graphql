query IndexerList {
    indexers(first: 1000) {
        id
        url
        stakedTokens
        allocations {
            createdAt
            status
        }
    }
    provisions(first: 1000) {
        indexer {
            id
        }
        tokensProvisioned
    }
}
