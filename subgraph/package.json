{
  "name": "graph-horizon-network-subgraph",
  "version": "0.1.0",
  "description": "Network subgraph for indexing Graph Horizon protocol",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy",
    "test": "graph test"
  },
  "devDependencies": {
    "@graphprotocol/graph-cli": "0.98.1",
    "@graphprotocol/graph-ts": "0.38.2",
    "matchstick-as": "0.6.0"
  }
}
